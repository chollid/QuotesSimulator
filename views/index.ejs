<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My App</title>
</head>
<body>
    <h1>May Node and Express be with you.</h1>
    
    <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">Submit</button>
    </form>
    <h2>Quotes</h2>
    <ul class="quotes">
        <% for(let i = 0; i < quotes.length; i++) {%>
        <li class="quote">
            <!-- Output name from the iterated quote object -->
            <span><%= quotes[i].name %></span>
            <!-- Output quote from the iterated quote object -->
            <span><%= quotes[i].quote %></span>
        </li>
    <% } %>
    </ul>
    <div>
        <h2>Replace a Quote.</h2>
        <p>Replace a quote given by someone famous with your own! (Hint: sends a PUT request!)</p>
        <button id="update-button">Replace Steve Job's Quoute</button>
    </div>
    <div>
        <h2>Remove Steve Jobs Quote</h2>
        <p>Delete one of Steve Jobs' quotes. Does nothing if there are no more Steve Jobs' Quotes left.</p>
        <button id="delete-button">Delete Quote</button>
    </div>

    <script src='/main.js'></script>
</body>
</html>